name: Insert Commit Message To Notion Datebase
run-name: ${{ github.actor }} 가 실행중입니다.🚀
on:
    schedule:
        - cron: "0 1 * * *"
jobs:
    Explore-GitHub-Actions:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v2

            - name: module install
              run: npm install

            - name: Run JavaScript
              run: npm start ${{secrets.NOTION_KEY}} ${{secrets.NOTION_DATABASE_ID}}
